[build]
  command = ""      # no build step
  publish = "."     # or "site" if index.html is in /site[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
