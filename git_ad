git add .
git commit -m "Convert to pure static site: HTML/CSS only (no JS/JSON)"
git pushgit add .
git commit -m "Add fixed gradient layer + force transparent panels"
git pushgit add .
git commit -m "Force gradient body + transparent containers for clean home"
git pushgit add public/_redirects public/_headers public/robots.txt public/sitemap.xml
git commit -m "Added redirects, headers, and sitemap configs"
git push origin maingit add -A
git commit -m "Home: BSS orange→black gradient, remove Maximax text, keep slogans"
git push
